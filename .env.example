# WorkOS Auth
WORKOS_API_KEY=sk_test_...
WORKOS_CLIENT_ID=client_...
VITE_WORKOS_CLIENT_ID=client_...

# Azure DevOps
AZURE_DEVOPS_ORG=your-org
AZURE_DEVOPS_PAT=your-pat

# App domain for subdomain routing
# Production: dashboard.velais.com → tenants at *.dashboard.velais.com
# Development: lvh.me subdomains are used automatically (no config needed)
APP_DOMAIN=dashboard.velais.com
VITE_APP_DOMAIN=dashboard.velais.com

# Dev-only: override tenant slug when running on localhost (no subdomain)
# Uncomment to simulate a specific tenant without using lvh.me subdomains
# VITE_DEV_TENANT_SLUG=foresound-srl

